# 项目工作约定

本项目工作区为 `E:\ai-customer-service-plan`。本文件集中维护项目执行约定；目录 README 提供导航，工程 README 提供可运行命令，主规格定义产品约束。

## 任务与资料读取

- 缺少项目上下文、涉及跨目录、版本交付或归档时，读取根 README 的相关部分。已有上下文可复用，不要求每次改动重读。
- 局部任务先定位受影响文件和最近的工程说明，再按接口、数据、权限或状态依赖读取相关规格章节。资料目录与引用表是检索入口，不是必须顺序通读的清单。
- 审查和诊断交付发现与证据；实施任务完成已授权的改动及适用验证。已授权范围内的常规实现选择不重复确认；确需新的业务输入或授权时，只暂停依赖该决定的部分，完成可独立推进的工作。
- 当前没有项目级 Skill。仅在需要重复执行专门流程时建立 Skill，写明触发条件、所需资料和完成标准；不复制全局通用规则，也不因模型版本变化删除质量约束。

## 验证与完成

验证按[开发目录的任务矩阵](E:/ai-customer-service-plan/开发/README.md)选择；工程 README 的命令列表不是每次改动全跑的清单。新增变更、失败或尚未解决的风险才触发扩大或重跑验证。

任务完成需同时满足：

1. 已交付用户要求的结果及其必要配套，没有遗留可在授权范围内完成的必需工作。
2. 适用验证完成；关键验证受环境或输入限制时，说明未验证能力与影响，标记待验证，不用低层测试替代，也不宣称对应审计项关闭。
3. 仅更新因本次变化而失实的说明、规格、导航或状态；交付说明包括结果、验证和仍存在的限制。

G0—G4、AC／NF、真实调用预算及公开准入约束在相应能力启用、交付或验收时适用。与局部任务无关的阶段条件不阻断本地文档、假供应商或隔离测试工作；高风险能力的相关验证仍是该能力完成与验收的必要条件。

## 提交、冻结与归档

当前工作区没有 Git 元数据。普通任务以改动文件和验证结果交付，不以创建仓库、提交或压缩包为完成前提；用户要求版本管理时再初始化。已有 Git 时，按任务约定提交相关变更并运行适用验证，不附加逐次冻结要求。

前端 v0.3 冻结包与原冻结记录保持原文。后续前端业务开发需有用户授权，授权后作为后续版本迭代，日常改动按风险验证；仅更新规则和说明不要求重建业务版本。独立审计、完整构建与回归、版本快照和哈希校验在再次冻结或交付发布包时执行，不逐次触发。本条是用户确认后的现行流程，替代旧冻结记录中将完整流程用于每次源码改动的表述；历史记录不覆盖写入。

## 资料整理

新产物按用户指定目录或以下职责存放：

- 产品：需求、业务规则、交互与产品验收。
- 归档：历史快照、修订记录与阶段交付证据。
- 架构：技术方案、数据与接口设计、状态协议和架构决策。
- 开发：应用工程、迁移、开发任务和测试。
- 审计：审计报告、问题跟踪和复验结果。
- 拓扑蓝图：可编辑图稿、部署关系、时序、数据流和网络图。
- 运维：部署运行、监控、备份恢复、故障处置及客服运营。

现有主文档在原路径维护，不另建重复的权威正文。移动文件时更新受影响的现行链接；历史记录中的旧路径通过现行索引说明，不重写原始证据。纯内部重构、注释或测试补充，不自动触发规格和修订记录更新；外部行为、接口、数据迁移、运行命令、交付范围或验证状态变化时更新实际受影响的权威资料。只为需要导航的入口维护索引，不要求每个源码文件登记到 README。

版本快照和用户提供的原始审计报告保持原文；更正、整改与复验在后续记录中说明。规格已采纳、功能已实现、测试已验证及实际已上线分别记录。真实密钥、口令和客户数据不写入示例、图稿与说明文档。
