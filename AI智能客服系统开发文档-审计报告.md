# AI 智能客服系统开发文档审计报告

审计日期：2026-09-05

修订跟踪：当前开发文档为 v1.3。第 1—5 节保留 v1.0 审计结论，第 6 节保留 v1.1 采纳记录；行号链接分别指向对应历史快照。产品经理审计后的修改见 [v1.2 修订记录](E:/ai-customer-service-plan/AI智能客服系统-v1.2修订记录.md)，后续技术实现审计的采纳见 [v1.3 修订记录](E:/ai-customer-service-plan/AI智能客服系统-v1.3修订记录.md)。

审计对象：[AI 智能客服系统开发文档 v1.0](E:/ai-customer-service-plan/versions/AI智能客服系统开发文档-v1.0.md)

原文快照 SHA-256：`AC564BBEAF8CEECE3A566D838995601797D02184E15DBB5612DAFEF6C76B6D96`

## 1. 结论与范围

文档可用于理解产品、立项和开展 V0 原型验证。若要把它作为开发人员可直接实现、测试人员可据此验收的规格，还需要补齐 6 项设计约定：2 项 P1、4 项 P2。

P1 表示涉及费用或数据生命周期，需要在相应功能处理真实客户数据前解决；P2 表示需要在相应模块上线前明确，以防实现分歧、返工或错误验收。这里的优先级针对设计修订，不代表已经确认存在运行中的漏洞。

本次逐节审阅了原文，核对了功能分期、接口、表结构、异常流程、资料生命周期、训练边界、费用计算与验收条款，并复核了相关官方资料。验证了两个 JSON 示例可解析、代码围栏配对、费用算例及工期加总。

本次仅审计文档，没有应用代码、实际 API 账单、供应商账号或运行环境可供验证。因此不作实际安全性、性能、缓存命中率或训练效果的背书。原开发文档未修改。

## 2. 发现汇总

| 编号 | 优先级 | 问题 | 最迟解决阶段 |
| --- | --- | --- | --- |
| A01 | P1 | 单次客户提问缺少累计调用与费用上限 | V1 建立预算基础；V3 接工具前完成循环上限 |
| A02 | P1 | 删除会话与后台任务之间缺少阻止回写的约定 | V1 删除功能；V2 扩展到 Worker |
| A03 | P2 | 固定会话版本没有衔接紧急停用与回滚 | V1 配置版本上线前 |
| A04 | P2 | 重新生成后没有定义哪一版回答进入后续上下文 | V1 重试功能上线前 |
| A05 | P2 | 访客凭证与业务账号的绑定规则尚未定义 | V2 公开试点前；V3 个人数据查询前 |
| A06 | P2 | 性能验收未定义正常负载下的失败率和完成率门槛 | V1 验收前 |

## 3. 详细发现

### A01 · P1：对单次调用设限，不能替代对一次客户提问的累计设限

位置：[预算控制，第 573 行](E:/ai-customer-service-plan/versions/AI智能客服系统开发文档-v1.0.md:573)；关联[多次调用说明，第 555 行](E:/ai-customer-service-plan/versions/AI智能客服系统开发文档-v1.0.md:555)和[用量表，第 290 行](E:/ai-customer-service-plan/versions/AI智能客服系统开发文档-v1.0.md:290)。

原文已经要求单条输入、输出、上下文、频率、并发及日／月预算，并且明确提出原子预留额度。本项不重复要求这些已经存在的措施。

缺少的是：以“一次客户提问”为单位，把摘要、问题改写、模型生成、工具后的再次生成、重试等操作归入同一成本边界；同时限制累计调用次数、工具循环次数、累计费用和总耗时。

**触发场景：** 模型连续要求查询工具，每轮输入和输出都在限制内，客户也只提交过一次消息。由于没有每次提问的调用上限，这个工作流仍可持续消耗，直到撞上全站日预算，影响其他客户使用。

**建议修订：** 引入贯穿整个提问处理过程的 `turn_id`，其下每个实际计费操作具有独立 `operation_id`、类型和供应商请求 ID。区分 turn、生成 run、实际网络 attempt；所有辅助调用和重试都归账。设置每 turn 的金额预算、累计 Token 预算、模型调用数、工具调用数、重试数和墙钟时间上限。为输入、输出及各计费操作预留费用，失败或取消时处理已消费和仍待核对的额度，不能将不确定账单立即清零。命中上限后停止新的外部调用并返回明确状态。

用量规范应明确普通输入、缓存读取、缓存写入及输出的计费字段映射；推理用量如已包含在供应商的输出总量中，不能重复相加。当前 OpenAI 部分模型区分缓存写入与读取价格，单一“缓存用量”不足以直接确定费用。[官方缓存计费说明](https://developers.openai.com/api/docs/guides/prompt-caching)

**验收案例：** 模拟一个持续返回工具调用的模型，在预定次数、金额或时间阈值首先到达时终止；摘要和失败重试仍计入同一 turn；多个并发 turn 不能超额分配同一份可用预算；取消后仍能核对迟到的供应商用量。

### A02 · P1：删除会话时，仍在运行的任务可能继续生成衍生数据

位置：[删除流程，第 526 行](E:/ai-customer-service-plan/versions/AI智能客服系统开发文档-v1.0.md:526)；关联[流式保存流程，第 205 行](E:/ai-customer-service-plan/versions/AI智能客服系统开发文档-v1.0.md:205)和[删除接口，第 251 行](E:/ai-customer-service-plan/versions/AI智能客服系统开发文档-v1.0.md:251)。

原文已经列出了删除对象、备份删除标记与上游删除，也明确说明删除原始数据不等于模型遗忘。缺口在并发处理：删除过程没有与生成任务、导出任务、摘要及后续索引 Worker 建立一致的写入规则。

**触发场景：** 客户在模型生成或导出过程中删除会话。删除任务清理了当时存在的数据，但后台任务还持有旧内容，随后又保存回复、摘要或生成可下载的导出文件。界面显示删除后，数据可能仍被重新产生或交付。

**建议修订：** 删除请求先原子地标记 `deleting` 或不可恢复写入的删除状态，同时增加资源版本号或撤销任务写入权。新请求不得读取或创建工作；已运行任务在分批写入、发布流事件、生成下载链接之前重新检查删除状态和版本。异步清理保持可重试，并明确删除完成条件与失败状态。只保留必要、最小化的删除审计信息。反馈纠错、来源快照、数据集副本等也需通过血缘关系纳入清理或既有产物处置流程。

**验收案例：** 在流式回复中途、导出完成前、索引任务排队后分别删除来源数据；故意延迟 Worker 完成。删除生效后不得继续展示、回写或提供下载；进程重启、任务重投与备份恢复后再次核验。

### A03 · P2：固定会话版本缺少被撤销后的处理规则

位置：[会话绑定配置的执行流程，第 203 行](E:/ai-customer-service-plan/versions/AI智能客服系统开发文档-v1.0.md:203)；关联[会话表，第 287 行](E:/ai-customer-service-plan/versions/AI智能客服系统开发文档-v1.0.md:287)和[回滚交付要求，第 636 行](E:/ai-customer-service-plan/versions/AI智能客服系统开发文档-v1.0.md:636)。

绑定版本有利于追踪历史行为，但文档没有区分“记录当时使用的版本”和“该版本现在是否还允许执行”。回滚入口的作用范围也没有覆盖已经存在的会话。

**触发场景：** 某助手版本出现错误业务承诺后，管理员把默认版本回滚。新会话使用安全版本，已有会话仍绑定问题版本；按照第 203 行继续读取原版本，就可能继续发生同类问题。

**建议修订：** 为助手版本和模型部署版本增加当前可运行状态与撤销原因；每次执行前检查，而不只在创建会话时检查。规定被撤销版本的会话是暂停、转人工，还是迁移到批准的替代版本。保留原始绑定与每次实际执行版本，迁移过程记录审计。跨供应商替换继续遵守原文的数据处理审批边界。

**验收案例：** 先创建绑定问题版本的会话，再撤销该版本；旧会话继续发言不得调用撤销版本。回滚后仍能重现历史记录，且不能绕过停用状态通过 retry 再次执行旧版。

### A04 · P2：重新生成没有定义对话分支与有效回答

位置：[重新生成规则，第 230 行](E:/ai-customer-service-plan/versions/AI智能客服系统开发文档-v1.0.md:230)；关联[重试接口，第 248 行](E:/ai-customer-service-plan/versions/AI智能客服系统开发文档-v1.0.md:248)和[消息与任务表，第 288 行](E:/ai-customer-service-plan/versions/AI智能客服系统开发文档-v1.0.md:288)。

原文已正确区分重复提交和主动重新生成，并要求保留旧结果、记录 `retry_of_run_id`。但这些记录只能表达执行关系，没有规定后续请求使用旧答案还是新答案，也没有定义在较早轮次重新生成时如何处理后续消息。

**触发场景：** 同一个问题先得到 A1，重新生成得到 A2，用户再问“按你刚才第二步操作可以吗”。如果按消息时间读取，两版答案可能同时进入上下文，或用户看到 A2 而后台引用 A1，产生解释与操作不一致。更早轮次的重试还会改变后续消息所依赖的上下文。

**建议修订：** V1 可以采用简单约束：仅允许当前会话末轮重新生成；答案关联 `reply_to_message_id` 与 `run_id`，为该用户消息指定当前有效答案，历史版本保留但默认不进入新上下文；流式失败、取消与过期答案的上下文参与规则明确化。若要允许任意历史轮重试，则增加父消息或分支结构，后续消息跟随所选分支。重试请求还需校验目标轮是否仍是预期会话末端。

**验收案例：** 在 A1、A2 内容明显不同的情况下继续追问，检查界面和模型输入使用同一版本；较早轮重试明确拒绝或创建分支，不得静默混入当前对话；重试进行时禁止另一个请求绕开会话顺序检查。

### A05 · P2：访客身份与业务身份之间缺少可实现的绑定协议

位置：[V2 访客凭证，第 108 行](E:/ai-customer-service-plan/versions/AI智能客服系统开发文档-v1.0.md:108)；关联[内部登录接口，第 238 行](E:/ai-customer-service-plan/versions/AI智能客服系统开发文档-v1.0.md:238)和[查询本人工单，第 357 行](E:/ai-customer-service-plan/versions/AI智能客服系统开发文档-v1.0.md:357)。

原文已经要求后端鉴权、会话归属与本人数据访问。尚未定义访客凭证如何签发、包含什么范围、何时过期和撤销，以及匿名聊天身份怎样绑定到经过业务系统验证的客户身份。

**触发场景：** 网站挂件允许匿名聊天，开发人员再实现“查询本人工单”时，没有统一的可信身份来源。如果各接口自行把会话 ID、客户端上报的用户 ID 或已知工单号视为身份，可能破坏原文要求的本人访问边界。文档不足以验证不同模块会采用同一种规则。

**建议修订：** 明确匿名访客与已验证客户两类主体；服务端签发或验证限定工作空间、会话范围、有效期的凭证，并规定凭证存放、撤销与失窃后的处理。匿名主体只访问允许的公共内容与自己的聊天范围。个人业务查询必须绑定业务系统签发并由后端验证的身份，资源查询再校验该身份的业务权限。网页挂件还需按嵌入方式明确跨域、Cookie/令牌与消息通信的信任边界，域名允许列表不能替代身份认证。

**验收案例：** 访客 A 不能读取 B 的会话；自报 customer_id 不改变身份；仅知道工单号不足以读取他人记录；过期和撤销凭证不可继续调用；登录后的身份绑定不能自动吞并其他访客的会话。

### A06 · P2：记录错误率不足以构成性能验收标准

位置：[并发与延迟验收，第 494 行](E:/ai-customer-service-plan/versions/AI智能客服系统开发文档-v1.0.md:494)；关联[测试条件，第 502 行](E:/ai-customer-service-plan/versions/AI智能客服系统开发文档-v1.0.md:502)。

原文列出了 20 并发和首字延迟 p95 ≤ 5 秒，并要求记录错误率，也明确这些是初始目标。缺少正常目标负载下可接受的错误率、完整回复完成率、测试持续时间与样本数，以及用户主动取消和故障注入是否计入分母。

**触发场景：** 大部分请求失败，少数成功请求很快返回，成功样本的首字 p95 仍达标。现有验收表没有正常负载下的错误率门槛，难以据此判定整项不通过。

**建议修订：** 冻结模型、配额、输入输出规模、并发方式、时长和样本量；正常负载测试明确成功率、完整回复率、超时率和延迟门槛；故障注入测试单列，验证失败状态与恢复，不混入正常负载指标。明确供应商故障的归因方式，但对客户可见的失败仍要统计。补充每次提问的费用上限与预算拦截验收，与 A01 对齐。

**验收案例：** 构造“大量失败但少数请求快速返回”的报告，必须判定为不通过；所有预算与成功率指标都能从原始运行记录复算。

## 4. 已核实且不应误判为问题的内容

| 项目 | 审计结果 |
| --- | --- |
| API 聊天、保存对话、RAG、微调的区分 | 概念边界合理；不需要把所有场景改成微调 |
| 服务端保存 Key、凭据不进前端持久存储 | 已有明确要求，不是遗漏 |
| SSRF、Markdown 清洗、检索权限过滤 | 原文已有覆盖；是否有效需后续检查实现 |
| 原子预算预留 | 第 538 行已经提出；缺口是调用层级与生命周期细化 |
| 重试去重、取消可能仍收费 | 已明确说明，不能指称文档完全没有考虑 |
| 人工接管后停止 AI 回复 | 已明确规定，且列出并发接管测试 |
| 微调数据分组、独立测试和人工审核 | 已有设计；实际样本数量与效果仍需验证 |
| OpenAI 自助微调限制 | 第 389 行日期与本次读取的官方时间表一致。[官方时间表](https://developers.openai.com/api/docs/deprecations) |
| 数据默认不用于训练但仍可能保留 | 表述保留了必要区别，与官方说明相符。[官方数据控制](https://developers.openai.com/api/docs/guides/your-data) |
| 费用算例 | 1,200 万输入、240 万输出；示例价格下合计 21.6，计算正确且已标为假设 |
| 开发工期加总 | 2+7+10=19；4+12+18=34，计算正确。是否做得到需根据团队和真实范围评估 |
| JSON 与代码围栏 | 两个 JSON 示例可解析；六组代码围栏成对 |

无需把原文明确标注的示例价格、工期估计和初始验收目标当作供应商承诺。当前资料也不足以据此断定预算或工期必然失真。

## 5. 建议的修订顺序

1. 在第 6、8、14 节补齐 A01 的 turn/run/attempt 关系、全链路费用归账和累计限额。
2. 在第 6、7、13 节补齐 A02 的删除状态、任务写入规则与删除完成条件。
3. 在第 5—8 节明确 A03 的版本撤销，以及 A04 的重试答案选择。
4. 在第 12 节把 A01—A04 的行为和 A06 的负载指标写成可以判定通过／失败的验收案例。
5. V2 公开试点前补齐 A05；V3 工具接入前再次验证身份绑定与每次提问的工具预算。

本报告提供修订依据，未修改原规格或创建应用代码。修订后的文档应形成新的版本与哈希，再由对应实现和验收结果验证这些问题是否真正解决。

## 6. v1.1 采纳记录（2026-09-05）

用户已授权采纳全部 6 项建议。[v1.1 开发文档快照](E:/ai-customer-service-plan/versions/AI智能客服系统开发文档-v1.1.md)已将约定同步写入业务流程、接口、数据表、成本策略及验收，而非仅在末尾附加建议。

v1.1 文档 SHA-256：`213B884229DA2CC5E7AE67C435952210A6FB3660C445EF7FD4852D2D44602B06`

| 编号 | 已落实的规格 | v1.1 快照定位 | 状态 |
| --- | --- | --- | --- |
| A01 | turn/run/operation/attempt 归属、所有尝试累计、金额及次数上限、unknown 预留、对账与费率映射 | [第 14.2 节](E:/ai-customer-service-plan/versions/AI智能客服系统开发文档-v1.1.md:707)；第 6、8、12 节同步 | 文档已补齐，待实现验收 |
| A02 | deleting/purged、write_epoch、发布闸门、删除 job、下载撤销、产物血缘和备份恢复顺序 | [第 6.4 节](E:/ai-customer-service-plan/versions/AI智能客服系统开发文档-v1.1.md:258)；第 7、8、13 节同步 | 文档已补齐，待实现验收 |
| A03 | 当前运行授权、版本撤销、默认回滚、旧会话暂停与授权迁移 | [第 5.3 节](E:/ai-customer-service-plan/versions/AI智能客服系统开发文档-v1.1.md:200)；接口与数据库同步 | 文档已补齐，待实现验收 |
| A04 | 只允许末轮重试、head/revision 校验、有效答案指针、失败不覆盖、重试沿用预算、不重放写入 | [第 6.3 节](E:/ai-customer-service-plan/versions/AI智能客服系统开发文档-v1.1.md:248)；接口与数据约束同步 | 文档已补齐，待实现验收 |
| A05 | 访客凭证期限与轮换、服务端范围、可信业务身份绑定、nonce 防重放、撤销及个人历史保护 | [第 13.4 节](E:/ai-customer-service-plan/versions/AI智能客服系统开发文档-v1.1.md:654)；第 7、8 节同步 | 文档已补齐，待实现验收 |
| A06 | 20 并发、样本数与时长、99% 成功及完整回复门槛、延迟口径、故障分组与复算要求 | [第 12.3 节](E:/ai-customer-service-plan/versions/AI智能客服系统开发文档-v1.1.md:591)；第 12.2、12.4 节同步 | 文档已补齐，待实现验收 |

第 12.4 节为每项问题增加了两组必测场景；第 17 节提供开发文档内的追踪表。原工期区间保留为 v1.0 历史规划基线，明确要求按本版新增细化工作复估，不将旧工期直接作为新承诺。

文档核对：三个 JSON 示例可解析；七组代码围栏成对；原费用示例与历史工期加总未改动；v1.0 快照哈希与审计基线一致，原始行号引用保留。接口与字段已覆盖上述新约定。

以上是文档层面的落实记录。尚未开发或运行相应系统，未执行第 12 节的并发、身份、删除、预算或故障注入测试；只有实现后取得相应证据，才能把“待实现验收”改为实际关闭。
